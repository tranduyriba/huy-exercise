prompt("Nhập tuổi của bạn")

alert("Alo")

// setTimout sẽ chỉ chạy 1 lần sau x giây
setTimeout(() => {}, 1000)

// Hàm setInterval sau 2s sẽ chạy tiếp các lần tioeeps theo
setInterval(() => {}, 2000)